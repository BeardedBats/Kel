# 02 — Skeleton Matrix (28 bones, post-release audit)

Status counts: **PASS 23 · PARTIAL 4 (23, 24, 25, 26) · FAIL 1 (19) · UNVERIFIED 0 · NOT_APPLICABLE 0.**
"Runtime evidence" is evidence observed executing, not read: the 363+10 engine suite (working tree and
clean clone of `v1.4.0`), `verify-release.ps1` 3/3, `verify_engine_pyz.py` 33/33, the 18 capture
manifests (0 console errors), and source-level traces of the packaged artifacts.

| Bone | Status | Intended | Actual | Source Evidence | Runtime Evidence | Drift | Recommended Action |
|---|---|---|---|---|---|---|---|
| 1 Commander / executive ownership | PASS | Kel interprets intent, decides direct vs delegate, decomposes, sequences, recovers, synthesizes; delegation never transfers final responsibility | Commander plans/validates/reviews; engine claims milestones, schedules reviews, composes combined-result milestone; only the engine publishes | `runtime/kel/commander.py`; `engine.py` tick/claim/review; `core.py` consume→assess→publish | engine suite (test_core, test_v1, test_review_recovery); g4/g5 work-center captures | None material | Keep |
| 2 Deterministic initial classification | PASS | Cheap deterministic signals for family/difficulty/continuation/routing hints | `router.classify()` wired into submit (the old D-04 dead code is fixed); continuation/knowledge/coding signals | `runtime/kel/router.py`; `service.py:139` | test_service_routing.py in suite | None | Keep; extend signals only if routing quality demands |
| 3 Capability-aware routing | PASS | Selection on capability, tools, class, quality, speed, cost, health, auth, quota, limits, failures, session, workspace | Hard-filter then cost/latency/quota ordering; readiness reason chains; review outcomes feed quality samples | `router.select`; `providers.py:167-196`; `core.record_review` | test_v14_providers.py; test_v12_reviewer_diversity.py | None new | Keep; quality data is sparse until ≥3 samples (by design) |
| 4 Worker classes | PASS | Distinguish direct Kel work, internal bounded workers, external full agents; don't treat full agents as raw completions | Three classes exist: internal leaf adapter (2 tools, budgets), native text slice (read-only, tools disabled), coding full-agent path on isolated snapshots | `internal.py`; `native.py`; `coding.py:33`; `host_runtime.py` | test_host_runtime, test_coding_transport, test_acp_host | Containment claims → Bone 19 | See D-01/D-02 |
| 5 Recursive delegation control | PASS | No uncontrolled worker-spawns-worker trees; default Kel → Worker ×3, not arbitrary trees | No subagent tools; `spawn_agent` tool-use is denied (test asserts); native CLI multi-agent/computer-use features disconnected; engine pool cap 2, one milestone per job | `internal.py` tool allowlist; `native.py` `--disable` list; `engine.py` concurrency; TEAM_MODEL.md:65-66 | test_core.py:381 (`spawn_agent` denied); suite | TEAM_MODEL "hard engine check" reads as doc-only; mechanism is structural | Reword doc in V1.5 docs pass |
| 6 Work contracts | PASS | Structured handoff: objective, deliverables, acceptance, constraints, refs, workspace, permissions, budget, verification plan | `validate_contract` enforces objective/milestone/checks; coding/document/research compilers add domain plans; composer supplies sourced context | `core.validate_contract`; `coding.compile_coding`; `composer.py` | test_compiler_scope, test_coding_boundaries, test_v13_composer | None | Keep |
| 7 Native session continuity | PASS | Supported agents preserve native sessions across attempts/restarts | `native_session` persisted per run; passed to retry/resume; argv `resume <id>`/`--resume`; session shape validated | `engine._execute`; `native.py`; `continuation.plan_resume` | test_v13_continuation*; CONT-* in suite | None | Keep |
| 8 Conversation vs work lifecycle | PASS | Turn end ≠ work end; side questions don't kill work; results return into durable state; restart-safe | Detached per-run brokers keep work alive; engine ticks independent of chat; approvals survive restart; auto-resume semantics recorded | `runner.py:123-200`; `engine.py` tick; `AUTO_RESUME.md` | test_broker_recovery.py; AUTO_RESUME release record | Close-path bounded kill (limitation, state-safe) | Deterministic close in V1.5 (Doc D-11) |
| 9 Durable work / event model | PASS | Persisted facts; status derived from facts, not mutable strings; restart survival | Append-only events (revision-unique), rebuildable projection, run inbox receipts, brokers table, ORPHANED fencing, native process registry | `core.py` events/rebuild/recover; `runner.py` | test_core P22; test_broker_recovery; test_coding_recovery; UPG-* | None | Keep |
| 10 CompletionContract compiler | PASS | Meaningful requests compiled to explicit completion requirements pre-execution | Contract model: objective, milestones, acceptance checks per milestone, constraints; compilers per kind; validation rejects missing checks | `core.validate_contract`; `compile_document`; `compile_coding` | test_compiler_scope.py; suite | None | Keep |
| 11 Completion authority | PASS | Worker cannot self-certify; evidence → checks → verdict by Kel | `consume` never maps worker success to verdict; `verify` re-checks digests/evidence; `assess` alone completes; publish separate | `core.consume/verify/assess/publish` | test_core.py (P21 changed-bytes invalidate; P22 disk failure); test_coding_boundaries | None | Keep |
| 12 Verification layers | PASS | Existence, state, behavior, regression, requirement-by-requirement, subjective quality; not hardcoded to files | Digests/manifests (existence/state); Kel-run tests + tests-preserved + source-stable (behavior/regression); checks per requirement; manual_review → independent reviewer (subjective) | `core.verify`; `coding.check_evidence`; `core.record_review` | test_coding_boundaries; test_research; test_failure_surfacing | None | Keep |
| 13 Independent review | PASS | Creator ≠ reviewer; reviewer output affects authority | Executor-as-reviewer rejected; review runs scheduled with attempts; reviewer diversity via alternates; solution reviews refuse self-certification | `core.record_review`; `engine._schedule_review`; `solution.py:278-279` | test_v12_reviewer_diversity; test_review_recovery | Single-provider installs review within one model family (documented residual) | Keep; note residual in docs |
| 14 Frozen successful milestones | PASS | Passing milestones are not redone after later failures | `revise` invalidates only changed spec/dependents; accepted milestones preserved across repair/resume; resume preserve/revalidate plan | `core.revise`; `continuation.plan_resume` | test_core P07, P20, scope_change_invalidates; suite | None | Keep |
| 15 Bounded retry and escalation | PASS | Limits, monotonic escalation, no storms, honest failure | attempts ≤4 per milestone; previous provider excluded on retry; 60 s/24 h circuits; WAITING_RESOURCE + route_block; honest FAILED/UNCERTAIN summaries | `engine.tick`; `core.provider_outcome`; `core.assess` | test_core; test_v14_providers | None | Keep |
| 16 Cheap-first with verification | PASS | Cheap capable workers first behind verification; quality-critical not blindly cheap | Cost-ordered selection behind hard gates; verification gate retains or escalates; quality floor supported; quality producer now exists | `router.select`; `core.record_review` quality samples | test_v14_providers; test_v12_reviewer_diversity | Old D-14/D-15 closed | Keep |
| 17 Failure recovery | PASS | Recover blockers safely, then resume the original outcome; repair ≠ completion | Fenced recovery (`recover_expired`, `recover_checked_code`, `recover_readonly_result`, `recover_prepared`); auth → 24 h circuit "Authentication needs repair"; repair prompt preserves accepted work | `core.recover*`; `runner` recovery paths; `apply_changes.recover_prepared` | test_broker_recovery; test_coding_recovery; test_isolated_recovery; test_apply_changes | None | Keep |
| 18 Provider health / failover | PASS | Real state model and routing integration; fallback reason recorded; unknown stays unknown | installed/authenticated/degraded/unavailable/quota/quota_not_reported; readiness reason + fallback chain; probes; health observations; engine retries on health recovery | `providers.py`; `engine.tick` retry_route | test_v14_providers; providers page captures (g6); quota_not_reported chip | None | Keep |
| 19 Permissions / capability leases | **FAIL** | Permissions centrally enforced on every execution path; leases, locked red lines, ask-once, destructive snapshot, emergency stop | Checker + schema complete and tested, but **zero non-API callers**: only `/api/autonomy` invokes it; coding host runs `danger-full-access`/`never`; role tool policies and `guardrail_decisions` never enforced/recorded; emergency stop revokes leases only | `autonomy.py` (checker); `service.py:582`; `host_runtime.py:37,45,49`; `guardrails.py`; `team.py` tool_policy data | suite passes AUTO-* as **checker-level** tests (test_v14_autonomy.py:20,76-90) | **P1 documented-but-unwired enforcement** | See D-01/D-02 (wire or correct docs; V1.4.1 minimum = doc truth) |
| 20 Deterministic recipes | PASS | Repeatable workflows as recipes; Kel not a static DAG engine | Recipe schema with step deps/cycle checks/permission requirements, builtins, preview/dry-run, draft-from-job; engine remains executive | `recipes.py`; `service.py:406` preview | test_v13_recipes; gate-5 action evidence (recipe-preview "Dry run", state unchanged) | None | Keep |
| 21 Memory / knowledge | PASS | Distinguish conversation/project/decisions/preferences/worker results/active work/knowledge; provenance/trust/correction; no personal-life memory | Trust ladder 1-7, untrusted external pinned 7, preference/decision gates, correction/retraction/conflicts, project scoping; recall reasons + fences | `memory.py`; `composer.py` | test_v13_memory; test_v13_composer; MEM-* | None | Keep |
| 22 One-voice synthesis | PASS | Worker outputs synthesized; conflicts/failures/uncertainty still feel like Kel | Engine publishes a single message; verification summary appended; combined-result milestone; reviewer disagreement → repair/UNCERTAIN, never raw dumps | `core.publish`; `engine` combined-result | suite; work-page "Kel verified" captures | None | Keep |
| 23 Observability without babysitting | PARTIAL | Orchestration complexity hidden by default; advanced detail optional | Default Kel chat shows "Automatic" (no model choice); Work drawer optional; BUT donor assistant-selection area is still mounted on the default guid page and donor surfaces remain reachable | `GuidPage.tsx:585-587`, `:668`; `Router.tsx`; `pages/team`, `pages/cron`, `pages/login` | g10 settings/route captures; 0 console errors | P3 donor-surface residue | Hide donor surfaces in V1.5; keep Kel surfaces |
| 24 Desktop productization | PARTIAL | One-assistant desktop experience across nav/onboarding/palette/keyboard/a11y/themes/notifications/tray/settings/diagnostics/active jobs | All mechanical areas present with evidence; identity leaks remain: tray tooltip `'AionUi'`, notification title `'AionUi'`, donor chrome | `tray.ts:265`; `useBrowserNotification.ts:66`; `Layout.tsx:138-161`; `KelWorkPanel.tsx` | G7/G9/G10 captures (a11y 30/30 rings, 0 contrast fails per record); suite | P3 identity leaks | Rename strings in V1.5 (or V1.4.1 polish); no vision verdict recorded |
| 25 AionUI donor usage | PARTIAL | Donor used deliberately; no donor concepts leak into the one-assistant illusion | Fork base 6744099 (v2.2.2, Apache-2.0); shell/services/updater/PWA adopted; donor pages kept in tree; donor binary bundled (`bundled-aioncore/win32-x64/aioncore.exe`, not in git); team/agent concepts gated but mounted | `desktop/KEL-FORK-NOTES.md`; `docs/v1/KEL_DONOR_LEDGER.md`; frozen `resources/` | frozen listing; asar contains V1.4 markers (`onboardingCompleted_v1`) | P3 donor residue + non-repo binary prerequisite | V1.5 donor-surface cleanup; document/attest aioncore provenance (D-09) |
| 26 Security boundaries | PARTIAL | Custody, IPC, export hygiene, guardrails, tamper detection | Custody solid (safeStorage; engine stores `credential_ref` metadata only; export allowlist + secret refusal; native env scrub) but enforcement gap (Bone 19), caller-supplied actor defaulting to `user`, bearer token at rest, `/api/apply` with no approval row, weak export test | `kelCredentials.ts`; `providers.py:199-213`; `diagnostics.py` `_sanitize`/EXPORT_ALLOWLIST; `service.py:559-561` | suite; SEC-* tests; verify-credentials script | **P1** enforcement (shared with 19); P2/P3 items | D-01/D-04/D-05/D-10 |
| 27 Diagnostics truthfulness | PASS | Only measured/known data; unknown stays unknown | Measured basis fields, `observed_at`, quota_not_reported exposed as such, DB integrity/size/WAL measured, no invented latency; version string labeled "Engine 0.5.0" (labeling nit) | `diagnostics.py` snapshot/measure; `providers.py`; `pages/kel/diagnostics` | test_v14_diagnostics; g8/g10 diagnostics captures | P3 version labeling | Align version strings (D-08) |
| 28 Packaging / release integrity | PASS | Frozen folder matches release; tag rebuilds; clean clone; engine/renderer current; migrations; no stale/uncommitted/hidden deps | Verified: frozen == dist/release (3/3 hashes, official script + independent); candidate == frozen artifacts; engine PYZ structurally identical 33/33; clean clone 363+10; UPG-* additive migrations; tag/main relationship exact (`main` = tag + release-record commit) | `scripts/freeze-release.ps1`; `verify-release.ps1`; `AUTO_RESUME.md`; git graph | verify-release run; clone pytest; verify_engine_pyz run | Notes: carried `debug.log`; sums lack filenames; aioncore unhashed; full from-scratch rebuild not attempted (not promised) | D-07/D-09 hygiene items; no blocker |
