# 12 — Performance (Kel V1.5)

Status: **skeleton** — measurements recorded with basis (Workstream 28). Existing spans live in
`kel.diagnostics`; numbers are recorded only when actually measured on a named build.

To track: engine startup · renderer startup · initial chat readiness · provider readiness · job
creation · routing latency · authorization latency · completion verification · diagnostics
overhead · memory-query latency.
