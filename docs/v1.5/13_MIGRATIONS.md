# 13 — Migrations (Kel V1.5)

Status: **skeleton** — the G11 matrix produces the results.

Paths to verify: fresh install · V1.4 data · V1.4.1 data. Areas: engine DB (migration 010
`v15-authorization` is additive: `guardrail_decisions`), config, provider state, credentials
metadata, jobs, memory, leases, roles, recipes, release state. No silent data loss.
