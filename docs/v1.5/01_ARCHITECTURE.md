# 01 — Architecture (Kel V1.5)

Status: **skeleton** — filled as V1.5 increments land. Authoritative gate board: `00_STATUS.md`.
Baseline truth: `docs/v1.4/KEL_V1.4_ARCHITECTURE.md` plus the V1.4.1 correction notes
(`docs/v1.4.1/`), which remain the last frozen, verified description of the product.

To be recorded here as gates close: the V1.5 runtime map (engine modules, broker processes, desktop
shell), the authorization flow (see `02_AUTHORIZATION_MODEL.md`), provider and credential runtime
(`03`/`04`), routing (`05`), and the exact runtime trust model statement.
