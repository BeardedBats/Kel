# 14 — Test Matrix (Kel V1.5)

Status: **working** — maps every gate and ledger row to its tests and runtime evidence.

Current citations (this working tree): `runtime/tests/test_v15_authorize.py` (24 tests: lease
lifecycle, actor binding, worker identity, expansion grants, role policy, engine claim gate,
application gate, coding adapter effect point, service identity); full-suite counts recorded in
`00_STATUS.md` after each increment.
