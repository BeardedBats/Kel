# 04 — PACKAGE EVIDENCE (Campaign C)

Status: NOT STARTED (runs late in the repair envelope, from the repaired head).

Planned bindings (Campaign C §22/§23 guard list):
- exact git SHA; renderer build inputs; engine build hash; staged runtime; packaged runtime; installer hash
- guards against stale Vite output, stale engine, wrong builder config, donor package metadata, nested runtime path, old cache, stale resources
- separate validation install root `C:\Users\Nick\KelRepairInstall` + separate data roots; Campaign B's `C:\Users\Nick\KelAuditInstall` preserved untouched
- install → launch → engine identity → fresh DB → upgrade DB → repaired paths → isolation → approvals → capabilities → provider behavior → memory → Needs Your Attention → recovery → repeated engine loss → package identity → About/branding → shortcuts → Add/Remove Programs → reinstall/upgrade → uninstall → cleanup → user-data retention

Results are appended when the battery runs.
