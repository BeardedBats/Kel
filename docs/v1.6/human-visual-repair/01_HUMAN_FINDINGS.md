# 01 — HUMAN FINDINGS (Kel V1.6 human visual review)

Source: Nick's human review of the audited V1.6 build (findings relayed 2026-09-19).
Every finding below is a repair item for this campaign. `HUMAN_VISUAL_GATE = PENDING_REVIEW`.

| ID | Finding | Root cause (confirmed from source) | Fix |
|----|---------|-------------------------------------|-----|
| HV-01 | Global contrast failure: dark/black foreground text on dark navy surfaces (Model settings rows, "Default Kel model" heading, System "Keep this computer awake", nested setting labels/states) | Kel components rendered inside donor settings pages inherit the UA default black text: `html/body` set background but no `color`; `.kel-card`/`.kel-h2` pin no foreground; only `.kel-scope` pages established a color. | Theme-boundary repair: base text color from theme token on `html, body`; pin `.kel-card` foreground; native control inheritance; verify Light/Dark/Follow-system. |
| HV-02 | Permissions page cannot scroll (no wheel/trackpad/scrollbar/PageUp/Down) | Kel pages have no scroll owner: `html/body/#root` are `overflow:hidden`; settings pages ship their own `overflow-y-auto` wrapper; `.kel-scope` pages (autonomy/work/…) do not. | `.kel-scope` becomes the single predictable scroll owner for Kel pages (flex child, `min-height:0`, `overflow-y:auto`, `overflow-x:clip`). |
| HV-03 | Work → "Open the chat" routes to Home instead of the referenced conversation | Action target is `/chat/<id>`; no such route exists; `*` fallback navigates to `/guid`. Existing system opens conversations at `/conversation/<id>`. | Point the attention action at the existing `/conversation/<id>` route; add regression coverage. |
| HV-04 | Sidebar shows generic black-square "K" instead of the canonical Kel logo; red attention indicator clipped at sidebar edge; "Work & context" footer detached/colliding | Layout header renders an inline black box + `<text>K</text>` SVG; badge offset `[10,-4]` pushes the indicator past the sider edge; footer spacing. | Use the canonical logo asset (same asset the About page loads); fix badge offset/clipping and footer hierarchy/spacing. |
| HV-05 | Work & context drawer exposes internal machinery: `RUNNING · UNCERTAIN`, `AWAITING_USER · UNCERTAIN`, simultaneous Pause/Resume/Cancel, "Access needed"/"Permission needed" duplication, dense tabs, content can exceed viewport | Drawer renders raw engine enums (`{job.state} · {job.verdict}`); action set not state-aware; tabs are internal-domain oriented. | Plain-language status translation (do not erase uncertainty), state-appropriate actions only, drawer scrolling, spacing/IA polish. |
| HV-06 | Permissions page normal-user language: "Capability leases", `job_review_summary`, "prepared review sample", "Ask the engine about a scope"; active permission showing `Expires: 0s ago` | Internal diagnostics occupy the primary Permissions surface; expiry formatting treats elapsed time as "expired" even for active leases. | Relabel to user language (Active permissions / Access requests); move internals behind advanced surface; fix expiry formatting truth. |
| HV-07 | Model page contradiction: built-in Claude/Codex shown "Available" while "No configured models" shows; badge readability; selected-row hierarchy | Built-in availability (engine) and custom-provider config (donor list) are presented as one undifferentiated surface; empty state wording conflates the two. | Explicit IA: "Available models" (built-in, truthful availability) vs "Custom models" (user-configured) + "No custom models configured"; badge contrast; row hierarchy; spacing. |
| HV-08 | System page: dark-on-dark heading (HV-01), excessive nested giant cards, uneven spacing; Work Directory exposes `…\kelwork\host\aionui` donor path | Card-in-card nesting; donor subfolder leaks into normal presentation. | Card reduction (sections/dividers/rows), spacing normalization; present the meaningful Kel workspace root; full physical path only in advanced/copy detail; no risky migration. |
| HV-09 | Appearance: Light theme preview contains "AionUI" donor branding; card-heavy; typography defaults allow Markdown 13 / Code 12 | Donor cover image shipped as theme preview; font-size defaults below Kel readability floors (markdown 13, code 12). | Replace donor preview with Kel-neutral asset; raise production defaults to floors (markdown 14, code 14, min 12); density pass. |
| HV-10 | Tools: `aionui-browser` donor name visible in MCP Tools Configuration | Built-in MCP server id used as display name. | Keep internal id (compatibility), Kel-native human label ("Kel Browser"); sweep nearby descriptions for donor terms. |
| HV-11 | Desktop Pet toggle shows ON until reload even though the pet cannot be created (RA-MINOR-003) | Bridge refuses silently (persists false, resolves normally), renderer never learns the enable failed. | Bridge rejects with a reason; renderer reverts + explains; discriminating regression test; no reload contradiction. |
| HV-12 | Team/Office/Roster/Studio page exposes internal workforce model ("Seed the default roster", roles) | Surface reachable via settings tabs ("Agents", "Team roles"), command palette entries, and direct routes. | Hide from ordinary navigation (remove entries, redirect routes) while preserving Workforce runtime; verify links after change. |
| HV-13 | Installer failure/repair dialogs can show donor `AionUi` naming (RA-MINOR-002/RA-SUG-002) | NSIS message scripts + fallback strings carry donor name. | Kel naming across installer messages and neighboring fallbacks; static/grep validation; replay smoke script. |
| HV-14 | Cross-cutting: button/control normalization, card reduction, responsive/readability matrix, accessibility basics | Mixed donor/Kel component styling. | Restrained polish pass on touched screens; verify at 1366×768 → 1920×1080 + narrow + 125% scale. |

Status legend: in progress / fixed (pending regression) / verified.
