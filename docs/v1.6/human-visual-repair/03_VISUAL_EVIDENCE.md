# 03 — VISUAL EVIDENCE (Kel V1.6 human visual repair)

Evidence directory: `docs/v1.6/human-visual-repair/evidence/` (fresh captures only; no reused renders).

## Required matrix (all touched surfaces)

Routes / surfaces to capture:

- Sidebar (expanded + collapsed) — canonical logo; attention indicator fully visible
- Home / chat (long markdown conversation open)
- Work (with attention items; populated + empty)
- Permissions — top / middle / bottom (proves scrolling works)
- Model settings — available vs custom clarity; badge contrast
- System settings — hierarchy; work-directory presentation
- Appearance — theme previews (Light / Dark / Follow system / custom) — no donor branding
- Tools — MCP list incl. built-in browser label
- Desktop Pet — truthful toggle states
- Remote / WebUI
- About — canonical logo, version
- Archived conversations
- Work & context drawer (chat) — statuses in plain language; scrolled to bottom
- Team route (post-disposition: must not be reachable in normal navigation)

Window sizes: 1366×768, 1440×900, 1440×960, 1920×1080, narrow (~1100×800), plus 125% scale spot-check if practical.

## Capture protocol

IMPLEMENT → BUILD → LAUNCH → CAPTURE → INSPECT → FIX → RECAPTURE.
Console errors must be captured alongside (zero unexplained renderer errors).

(Per-surface evidence file names and results are appended below as captured.)
