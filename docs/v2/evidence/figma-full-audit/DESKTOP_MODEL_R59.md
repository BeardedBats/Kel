# Desktop Dark Model comparison — r59

**Authority:** Kel Design System, [FINAL Model `188:1327`](key/figma-model-1440.png), Model panel `188:1563`, Foundations `139:2`, Input states `148:229`, and Modal surface `147:162`. **Package:** `C:\Users\Nick\KelV2Candidate.r59`, source `96d2b5d55185d5ec0bc112132c82ba7edac4932e`, Dark, 100% zoom. The `r43-populated` roots are disposable.

The [r57 1440px baseline](key/r57-model-1440.png) repeated “Default Kel model” inside the first card, boxed every row, used a Heartbeat glyph in the empty state, and placed Custom models at y505. FINAL calls the first card “Available now,” uses a blue selected row with bottom dividers, shows an activity glyph, and places Custom models at about y525.

The [r58 1440px package](key/r58-model-1440.png) measures Available now at x613/y183, 670×329; six rows start at x634/y230 and each measure 628×40. Custom models starts at x613/y524 and measures 670×215. These bounds match FINAL within about one pixel. The title, row padding, status sizes and colors, muted routing note, and empty glyph now use the Figma panel values. [The 800px package](key/r58-model-800.png) has no horizontal document overflow; its routing note wraps. [The action record](r58-model-actions.json) confirms a model choice, return to Automatic, and Add Model open and Escape close on the disposable profile.

The [r58 Add Model baseline](key/r58-add-model-dialog.png) had 30px inputs, 32px selects, a browser focus outline, and internal required messages such as `base_url is required`. The modal surface already used the Figma glass gradient and blur. The r59 [default](key/r59-add-model-default.png), [focus](key/r59-add-model-focus.png), and [error](key/r59-add-model-error.png) captures show 34px fields, one blue focus edge and glow, one red error edge and glow, and plain required messages. [Computed styles and actions](r59-add-model-check.json) confirm the values, Escape closure, and [800px fit](key/r59-add-model-800.png). No provider was submitted.

FINAL's empty Custom models card has no Add Model button. The package keeps that visible action because it is the entry point for a working flow. The fixture has one Recent item rather than FINAL's sample history. A populated custom model card, real provider connection, and live model response remain unverified. This pass does not establish exact product-wide parity.
