# Desktop Model — current Figma revision

Current Figma [Model `311:2239`](https://www.figma.com/design/BlpVvZGuc9j9HhxUojIiJI/Kel-Design-System?node-id=311-2239) was read directly. The source now uses its Model title and Default model rows: a leading icon, model and provider labels, availability, and Use or Set up actions. Its actual six-model catalog stays visible rather than replacing it with Figma's four sample rows. No custom model exists in the isolated profile, so the second card shows a real empty state rather than Figma's populated DeepSeek example.

An isolated Windows package showed the first card at x613/y183, 670×412 at [1440px](DESKTOP_MODEL_PACKAGE_1440.png) and x371/y179, 388×412 at [800px](DESKTOP_MODEL_PACKAGE_800.png). It showed no horizontal overflow or renderer error. Selecting Claude Sonnet 4.6 set the default, Automatic restored it, and DeepSeek Chat's Set up action opened Providers. These checks used only isolated data. The canonical App and Data were untouched.

The 800px column-width repair was verified in the final capture. The Add model dialog now follows Figma's compact Platform, API endpoint, API key, and Models flow at [1440px](DESKTOP_MODEL_ADD_PACKAGE_1440.png) and [800px](DESKTOP_MODEL_ADD_PACKAGE_800.png). It measured x440/y130, 560×448 at 1440px; Figma shows 560×440. DeepSeek starts with two checked model names. Its API key stays empty until the user enters one, and Kel does not claim a successful key check. The functional preset endpoint keeps `/v1`, which differs from Figma's illustrative URL.

A disposable DeepSeek provider with a fake key and loopback endpoint exercised the save path. The [1440px](DESKTOP_MODEL_POPULATED_PACKAGE_1440.png) and [800px](DESKTOP_MODEL_POPULATED_PACKAGE_800.png) captures show the real expanded provider and two models. The provider panel measured 670×209 and 388×209. Its provider and model switches changed off and back on; the options menu opened. A model Check attempted only the loopback endpoint and recorded a truthful failed status. The provider was then deleted through its confirmation, and a fresh package launch returned to the empty state. Neither width overflowed or reported a renderer error. A successful provider check and real credential remain unverified. Canonical App and Data were untouched.
