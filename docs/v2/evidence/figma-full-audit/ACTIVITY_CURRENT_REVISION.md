# Current Figma mobile Activity — scoped package evidence

The live [mobile Activity frame `299:14061`](ACTIVITY_CURRENT_FIGMA_299-14061.png) was read directly from the [Kel Design System](https://www.figma.com/design/BlpVvZGuc9j9HhxUojIiJI/Kel-Design-System). A disposable Windows package used an isolated engine job titled `Homepage refresh`. The engine Store created and claimed that synthetic job with milestone `Drafting step 1 questions`. No model worker executed.

| Width | Disposable package | Measured result |
| --- | --- | --- |
| 393 × 852 | [Capture](ACTIVITY_CURRENT_PACKAGE_393.png) | Happening now x16/y114, 361×104; Waiting x16/y230, 361×75; Recently finished x16/y317, 361×75. These match the current Figma card bounds. |
| 320 × 852 | [Capture](ACTIVITY_CURRENT_PACKAGE_320.png) | All cards fit x16–304 with zero document overflow. |

The row reads the real stored request, running milestone objective, and job state. It uses the current Figma mobile Card and row geometry. A RUNNING job no longer appears in “Waiting on you”; that card now reserves itself for actionable paused, approval, or interrupted work. The back control returned to Projects. No renderer error appeared. The isolated job was cancelled and acknowledged after capture; its SQLite WAL checkpoint returned `(0, 0, 0)`. TypeScript, the full desktop suite **54 files / 400 tests**, Electron Vite, and disposable Windows packaging passed. Canonical App and Data were untouched.

This is scoped populated-state geometry and state projection, **not a live model execution**. Windows titlebar and font rasterization differ from the phone frame. The fifth Kibble tab awaits a product decision. Other Activity states and project-specific scope remain open.
