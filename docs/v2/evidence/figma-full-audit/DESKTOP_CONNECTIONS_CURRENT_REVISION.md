# Desktop Connections — current implementation check

Current Figma [empty Connections `273:12085`](DESKTOP_CONNECTIONS_EMPTY_FIGMA_273-12085.png) was read directly. The package renders [the empty state at 1440px](DESKTOP_CONNECTIONS_EMPTY_PACKAGE_1440.png) and [800px](DESKTOP_CONNECTIONS_EMPTY_PACKAGE_800.png). The 1440px Services card measures x613/y183, 670×252, matching Figma's frame. The 800px card measures x371/y179, 388×252. Neither width overflowed or reported a renderer error.

The desktop settings navigation now includes Connections with Figma's plug icon. Saved services have their own card. Available known services appear separately when a saved service exists. With no saved service, the page shows the Figma empty state. The [Add form](DESKTOP_CONNECTIONS_ADD_PACKAGE_1440.png) opens above Services. Six main fields fit in two columns without overlap; framework and credential options remain behind Advanced options. Known service templates remain available through the form. The package opened the form but did not save a service, credential, or action.

Current Figma's populated list `269:5`, Add `269:501`, credential `269:971`, and action `269:1419` were inspected. A later disposable package used three synthetic service records and captured [the populated list at 1440px](DESKTOP_CONNECTIONS_POPULATED_PACKAGE_1440.png) and [800px](DESKTOP_CONNECTIONS_POPULATED_PACKAGE_800.png). The Services card measured x613/y183, 670×244 at 1440px, close to Figma's x613/y186, 670×240; each service row measured 56px. At 800px the card measured x371/y179, 388×244. Neither width overflowed or reported a renderer error. The three synthetic records were removed after each capture.

The [inline credential editor](DESKTOP_CONNECTIONS_CREDENTIAL_PACKAGE_1440.png) opens inside the selected service row. Its field shows the configured HTTP header, `Authorization`, while the credential field key remains internal. The package showed a masked empty input and did not submit a value. The card measured x613/y183, 670×394; it is taller than Figma because Kel keeps service options and a secure-storage note visible. The Add form still has a template disclosure and Advanced options, and its card remains taller than Figma's sample.

The source now places action confirmation below the action rows with Cancel and Yes, run it. A focused DOM test verifies that no mutating request runs before confirmation. The packaged engine has only documented read actions; it cannot produce Figma's illustrative Linear issue action, so the mutating confirmation frame has **no packaged visual check**. The package shows real engine action labels rather than inventing that issue action. A final CSS adjustment after the package reduced action-label crowding at 800px; that specific adjustment is not yet package-verified. TypeScript, the full desktop suite **54 files / 401 tests**, Electron Vite, and disposable Windows packaging passed. Canonical App and Data were untouched.
