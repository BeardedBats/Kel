/**
 * Node-lane vitest setup.
 *
 * The donor tree shipped this file alongside tests/unit|integration|regression.
 * Kel's strip removed those directories but kept the harness config, so this
 * file exists to keep the remaining `packages/web-host/src` tests runnable.
 * Keep it side-effect free: lane-specific needs belong in the test file.
 */
export {};
