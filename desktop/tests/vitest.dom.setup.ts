/**
 * jsdom-lane vitest setup.
 *
 * Counterpart of vitest.setup.ts for `*.dom.test.ts(x)`; Kel currently ships
 * no dom tests, so this file only keeps the project entry point resolvable.
 */
export {};
